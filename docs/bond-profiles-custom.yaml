# Custom Bond Profiles - Template v4.0
#
# Available Filters:
# - minPrice / maxPrice:       Price range in bond currency
# - minMatYears / maxMatYears: Years to maturity range (from today)
# - minSAY:                    Minimum Simple Annual Yield (%)  ← note: SAY not SAL
# - minYield:                  Minimum Current Yield (%)
# - minCapitalAtMat:           Minimum final capital at maturity (EUR)
# - minRating:                 Minimum credit rating (e.g., BBB+, A, AA-)
#
# RATING HIERARCHY:
# AAA > AA+ > AA > AA- > A+ > A > A- > BBB+ > BBB > BBB- > BB+ > BB > B+ > B
#
# VALIDATION:
# - Numeric values without quotes  (minSAY: 4.0 ✅   minSAY: "4.0" ❌)
# - minSAY not minSAL (common typo — filter silently ignored if wrong)
# - camelCase IDs, no spaces or hyphens
# ─────────────────────────────────────────────────────────────────────────────

profiles:

  # Capital target: selects bonds that return at least €1,600 from €1,000 invested.
  # Uses minCapitalAtMat — the only filter not covered by the default profiles.
  - id: highFinalCapital
    label: High Final Capital
    emoji: "🎯💎"
    description: Bonds returning at least €1,600 from €1,000 invested at maturity
    profileType: SAY
    sortedBy: CAPITAL_AT_MAT
    filters:
      minCapitalAtMat: 1600
      minMatYears: 3
      maxMatYears: 30
      minRating: BBB-

  # Near-maturity at par: short runway (1-4y) with price close to par (95-105).
  # Return is essentially certain — no mark-to-market risk, no pull-to-par speculation.
  # Different from cashParking (no price filter) and ultraShortHigh (high yield / no price filter).
  - id: nearMaturityPar
    label: Near Maturity Par
    emoji: "🏁💶"
    description: Short runway (1-4y) near par price (95-105). Predictable, low volatility return.
    profileType: SAY
    sortedBy: SAY
    filters:
      minPrice: 95
      maxPrice: 105
      minMatYears: 1
      maxMatYears: 4
      minRating: BBB