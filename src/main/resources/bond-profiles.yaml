profiles:
  # 1. LIQUIDITY / SAFETY (The "Bank Replacement")
  # Short term, high quality. Safe place to park cash.
  - id: cashParking
    label: Cash Parking
    emoji: "🅿️🛡️"
    description: Short (<3y) investment grade for liquidity
    profileType: SAY
    sortedBy: SAY
    filters:
      maxMatYears: 3
      minRating: BBB+
      minYield: 3.5

  # 2. SHORT-TERM YIELD (The "Aggressive Short")
  # Taking risk for high returns in the near term (1-3y).
  - id: ultraShortHigh
    label: Ultra Short High
    emoji: "⚡💰"
    description: Short-term (1-3y) bonds with very high yield (6%+)
    profileType: SAY
    sortedBy: SAY
    filters:
      minMatYears: 1
      maxMatYears: 3
      minYield: 6.0

  # 3. BALANCED CORE (The "Sleep Well" Profile)
  # Merged your 'balancedReturn' and 'sayConservative'.
  - id: balancedCore
    label: Balanced Core
    emoji: "⚖️🌲"
    description: Mid-term (5-15y), Investment Grade, >4.5% SAY
    profileType: SAY
    sortedBy: SAY
    filters:
      minMatYears: 5
      maxMatYears: 15
      minSAY: 4.5
      minRating: BBB+

  # 4. MAX INCOME (The "Cashflow Machine")
  # Based on your 'incomeHigh' - excellent for retirees living on coupons.
  - id: maxIncome
    label: Max Income
    emoji: "💵🔥"
    description: High current yield (>6%) with price protection (<110)
    profileType: income
    sortedBy: CURR_YIELD
    filters:
      maxPrice: 110
      minMatYears: 15
      minYield: 6.0
      minRating: BB+

  # 5. DEEP DISCOUNT (The "Capital Builder")
  # Modified your 'sayAggressive' to actually target "Cheap" bonds.
  - id: deepDiscount
    label: Deep Discount
    emoji: "📉🚀"
    description: Bonds trading <90% par. Profit comes from pull-to-par.
    profileType: SAY
    sortedBy: SAY
    filters:
      maxPrice: 90
      minMatYears: 3
      maxMatYears: 20
      minSAY: 5.0

  # 6. FORTRESS (The "Crisis Hedge")
  # Kept your 'topRated' - crucial for times of market panic.
  - id: fortressSafe
    label: AAA/AA Fortress
    emoji: "🏰🛡️"
    description: Top-tier sovereigns (AA-/AAA) only
    profileType: SAY
    sortedBy: SAY
    filters:
      minRating: AA-
      minSAL: 3.0
      minMatYears: 5
      maxMatYears: 30